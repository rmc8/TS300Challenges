/**
 * Challenge 176: Omit<T, K>
 *
 * Omit<T, K> を使って、特定のプロパティを除外してください。
 *
 * 学習ポイント:
 * - Omit<T, K>
 * - プロパティの除外
 * - 不要なプロパティの削除
 * - Pickの逆
 *
 * @example
 * challenge176();
 * // 出力: { name: 'Taro', age: 25 }
 */

export function challenge176(): void {
  // TODO: type Person = { name: string; age: number; email: string; address: string } を定義してください
  // TODO: Omit<Person, 'email' | 'address'> 型の変数を宣言してください
  // TODO: 除外されなかったプロパティを設定してconsole.log()で出力してください
}
