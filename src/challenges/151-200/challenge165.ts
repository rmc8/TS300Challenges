/**
 * Challenge 165: ジェネリックなコールバック
 *
 * ジェネリックなコールバック関数を定義してください。
 *
 * 学習ポイント:
 * - ジェネリックなコールバック
 * - callback: (item: T) => U
 * - map関数の型
 * - 変換関数の型安全性
 *
 * @example
 * challenge165();
 * // 出力: [2, 4, 6]
 */

export function challenge165(): void {
  // TODO: function map<T, U>(arr: T[], callback: (item: T) => U): U[] の形式で関数を宣言してください
  // TODO: arr.map(callback) を返してください
  // TODO: 数値配列を文字列配列に変換してconsole.log()で出力してください
}
