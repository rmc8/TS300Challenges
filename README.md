# TS300Challenges

300のTypeScriptコーディングチャレンジで学習と練習を行うためのリポジトリです。

## 概要

このプロジェクトは、TypeScriptの構文、型システム、アルゴリズム、問題解決スキルを実践的に学ぶための300個のチャレンジを提供します。難易度別に整理されており、初心者から上級者まで段階的に学習できます。

## セットアップ

```bash
# 依存関係のインストール
npm install

# プロジェクトのビルド
npm run build

# テストの実行
npm test
```

## チャレンジの構成

チャレンジは難易度別に6つのセクションに分かれています:

- **001-050**: 基礎レベル - TypeScriptの基本構文と型
- **051-100**: 初級レベル - 基本的なアルゴリズムとデータ構造
- **101-150**: 中級レベル - より複雑な問題解決
- **151-200**: 上級レベル - 高度な型システムとパターン
- **201-250**: エキスパートレベル - 複雑なアルゴリズムと最適化
- **251-300**: マスターレベル - 最高難易度の課題

## 使い方

各チャレンジは以下の構造になっています:

```
src/challenges/XXX-YYY/
├── challengeNNN.ts       # チャレンジの実装
└── challengeNNN.test.ts  # テストケース
```

### 個別のチャレンジをテストする

```bash
npm test -- challenge001
```

### 特定のテストを実行する

```bash
npm test -- -t "test name"
```

### コードの品質チェック

```bash
# リントの実行
npm run lint

# 自動修正
npm run lint:fix

# フォーマット
npm run format
```

## 開発ガイドライン

- すべての関数に適切な型注釈を付ける
- 各チャレンジには包括的なテストを含める
- TypeScriptの厳格モードに準拠する
- エッジケースを考慮する
- わかりやすい関数名と変数名を使用する

## 学習の注意

学習の上でも生成AIを使っても良いですがコードは必ず読み、コピーアンドペーストはせず写経し、コードの動作を検証して理解するように努めてください。コードを読まずコピーアンドペーストをするとコードの内容が理解できず、300問を終えても十分にTypeScriptを習得できない可能性があります。

## 参考資料

- [TypeScript公式ドキュメント (日本語)](https://www.typescriptlang.org/ja/docs/)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/handbook/intro.html)

## ライセンス

MIT

---

## Challenges

### 1. TypeScript入門 (10問)

1. "Hello, TypeScript!"をコンソールに出力するプログラムを書いてください。
2. number型の変数に10を代入し、その値を出力してください。
3. string型の変数に自分の名前を代入し、`私の名前は${名前}です`とテンプレートリテラルで出力してください。
4. 2つのnumber型変数を宣言し、それぞれに値を代入した後、その和を出力してください。
5. boolean型の変数を宣言し、trueを代入してから、その値を出力してください。
6. const と let の違いを示すプログラムを書き、再代入を試みてください。
7. any型とunknown型の変数をそれぞれ宣言し、異なる型の値を代入して違いを確認してください。
8. 型推論を利用して変数を宣言し（明示的な型注釈なし）、TypeScriptが推論した型を確認してください。
9. void型を返す関数を定義し、コンソールにメッセージを出力してください。
10. never型が使われるケース（常にエラーをthrowする関数）を実装してください。

### 2. 数値と演算 (10問)

1. 2つの数値の和、差、積、商、剰余を計算し、結果を出力してください。
2. Math.pow()を使って数値を2乗する計算を行い、結果を出力してください。
3. Math.sqrt()を使って数値の平方根を計算し、結果を出力してください。
4. Math.ceil()、Math.floor()、Math.round()を使用し、結果を出力してください。
5. インクリメント演算子（++）とデクリメント演算子（--）を使用して、変数の値を変更し出力してください。
6. 三項演算子を使用して、ある数が偶数か奇数かを判定し、結果を出力してください。
7. ビット演算（AND、OR、XOR、NOT）を行い、結果を出力してください。
8. Number.toString()を使って数値を2進数、8進数、16進数の文字列に変換し、それぞれ出力してください。
9. 複合代入演算子（+=、-=、*=、/=、%=）を使用して計算を行い、結果を出力してください。
10. Math.abs()を使って数値の絶対値を計算し、Math.max()とMath.min()で複数の数値から最大値と最小値を求めてください。

### 3. 文字列操作 (10問)

1. 2つの文字列を連結し（+演算子とconcat()メソッドの両方で）、結果を出力してください。
2. string型の変数の長さをlengthプロパティで取得し、出力してください。
3. toUpperCase()とtoLowerCase()を使って文字列をすべて大文字・小文字に変換してから出力してください。
4. trim()、trimStart()、trimEnd()を使って文字列の空白を削除してから出力してください。
5. replace()とreplaceAll()を使って文字列内の特定の文字や単語を置換してから出力してください。
6. split()を使って文字列を指定した区切り文字で分割し、結果の配列を出力してください。
7. startsWith()とendsWith()を使って文字列が特定の文字や単語で始まるか・終わるかを判定し、結果を出力してください。
8. includes()とindexOf()を使って文字列内に特定の文字や単語が含まれるかチェックし、位置を出力してください。
9. 文字列内の特定の文字の出現回数を数えるプログラムを書いてください（split()やmatch()を活用）。
10. テンプレートリテラルを使用して、複数の変数を含む複雑な文字列をフォーマットし、出力してください。

### 4. 条件分岐 (10問)

1. if文を使用して、数値が正か負か0かを判定するプログラムを書いてください。
2. if-else文を使用して、年齢が18歳以上かどうかを判定し、型注釈を付けてください。
3. else-if文を使用して、点数から成績（A、B、C、D、F）を判定するプログラムを書いてください。
4. switch文を使用して、曜日の名前を数字（0-6）から判定するプログラムを書いてください。
5. 論理演算子（&&、||、!）を使用して、複雑な条件を持つif文を作成してください。
6. 三項演算子を使用して、2つの数値の大きい方を選択し、型推論を確認してください。
7. ネストしたif文を使用して、数値が特定の範囲内にあるかどうかを判定するプログラムを書いてください。
8. strictNullChecksを有効にして、null/undefinedチェックを行うif文を含むプログラムを書いてください。
9. null合体演算子（??）を使用して、nullまたはundefinedの場合にデフォルト値を設定するプログラムを書いてください。
10. switch文でenumを使用し、型安全な分岐処理を実装してください。

### 5. ループ処理 (10問)

1. for文を使用して、1から10までの数を出力するプログラムを書いてください。
2. while文を使用して、1から10までの偶数を出力するプログラムを書いてください。
3. do-while文を使用して、少なくとも1回は実行されるループを実装してください。
4. for-of文を使用して、配列の全要素を出力するプログラムを書いてください。
5. break文を使用して、特定の条件でループを終了するプログラムを書いてください。
6. continue文を使用して、特定の条件をスキップするループプログラムを書いてください。
7. ネストしたfor文を使用して、九九の表を出力するプログラムを書いてください。
8. forEach()メソッドを使用して、配列の全要素を処理するプログラムを書いてください。
9. for文を使用して、フィボナッチ数列の最初の10項を配列に格納し出力するプログラムを書いてください。
10. for-in文を使用してオブジェクトのキーを列挙し、各プロパティの値を出力してください。

### 6. 配列の基礎 (15問)

1. number[]型の配列を作成し、型注釈を付けて全要素を出力するプログラムを書いてください。
2. push()とpop()を使って配列に要素を追加・削除し、型安全性を確認してください。
3. shift()とunshift()を使って配列の先頭の要素を削除・追加してください。
4. splice()を使って配列の特定の位置に要素を挿入・削除してください。
5. concat()またはスプレッド演算子（...）を使って2つの配列を結合し、結果を出力してください。
6. sort()を使って配列をソートしてください（数値配列と文字列配列の両方で）。
7. reverse()を使って配列を逆順にし、結果を出力してください。
8. Math.max(...array)とMath.min(...array)を使って配列の最大値と最小値を求めてください。
9. indexOf()とfindIndex()を使って特定の要素のインデックスを見つけ、出力してください。
10. includes()とsome()を使って配列が特定の条件を満たすかどうかを判定してください。
11. map()を使って配列の各要素を2倍にした新しい配列を作成してください。
12. filter()を使って配列から偶数のみを抽出した新しい配列を作成してください。
13. reduce()を使って配列の全要素の合計を計算してください。
14. every()を使って配列のすべての要素が特定の条件を満たすかチェックしてください。
15. ReadonlyArray<T>またはreadonly T[]を使って、変更不可能な配列を作成してください。

### 7. オブジェクトの基礎 (15問)

1. オブジェクトリテラルで型注釈を付けてオブジェクトを作成し、プロパティにアクセスして出力してください。
2. インターフェイスを使ってオブジェクトの型を定義し、そのインターフェイスに準拠したオブジェクトを作成してください。
3. オプショナルプロパティ（?）を含むインターフェイスを定義し、使用してください。
4. readonlyプロパティを含むインターフェイスを定義し、再代入を試みてエラーを確認してください。
5. オブジェクトの分割代入を使って、複数のプロパティを個別の変数に代入してください。
6. スプレッド演算子（...）を使って、オブジェクトをコピーまたはマージしてください。
7. Object.keys()、Object.values()、Object.entries()を使ってオブジェクトのキー・値・エントリを取得してください。
8. in演算子を使ってオブジェクトに特定のプロパティが存在するかチェックしてください。
9. Index Signature（[key: string]: T）を使って、動的なプロパティを持つオブジェクトの型を定義してください。
10. type エイリアスを使ってオブジェクトの型を定義し、インターフェイスとの違いを確認してください。
11. ネストしたオブジェクトを作成し、深い階層のプロパティにアクセスしてください。
12. オブジェクトのプロパティを削除する（delete演算子）プログラムを書いてください。
13. Object.assign()を使って複数のオブジェクトをマージしてください。
14. オブジェクトのメソッドを定義し、thisキーワードを使用してプロパティにアクセスしてください。
15. Computed property names（計算されたプロパティ名）を使ってオブジェクトを作成してください。

### 8. 関数の基礎 (20問)

1. 関数宣言（function）で型注釈を付けた関数を定義し、呼び出してください。
2. アロー関数（=>）で型注釈を付けた関数を定義し、変数に代入してください。
3. 引数と戻り値に型注釈を付けた関数を作成してください。
4. オプショナルパラメータ（?）を持つ関数を定義し、異なる引数の組み合わせで呼び出してください。
5. デフォルトパラメータを持つ関数を定義し、引数を省略して呼び出してください。
6. Rest parameters（...args: T[]）を使って可変長引数を受け取る関数を作成してください。
7. 関数オーバーロードを使って、異なる型の引数を受け付ける関数を定義してください。
8. 高階関数（関数を引数として受け取る関数）を作成してください。
9. 関数を返す関数（関数ファクトリー）を作成してください。
10. コールバック関数を受け取り、配列の各要素に適用する関数を作成してください。
11. map()、filter()、reduce()を組み合わせて配列を処理してください。
12. クロージャーを使用して、外部のスコープの変数にアクセスする関数を作成してください。
13. 即時実行関数式（IIFE）を使ってスコープを作成してください。
14. 再帰関数を使って階乗を計算するプログラムを書いてください。
15. 関数型（Function Type）を定義し、その型の変数に関数を代入してください。
16. void型を返す関数とnever型を返す関数の違いを示してください。
17. thisの型を明示的に指定した関数を作成してください（this parameter）。
18. アロー関数と通常の関数でのthisの違いを示すプログラムを書いてください。
19. bind()、call()、apply()を使って関数のthisコンテキストを変更してください。
20. カリー化（Currying）された関数を実装してください。

### 9. 型システムの基礎 (20問)

1. Union型（string | number）を使って、複数の型を受け入れる変数を宣言してください。
2. Intersection型（Type1 & Type2）を使って、複数の型を組み合わせた新しい型を作成してください。
3. 型エイリアス（type）を使って複雑な型に名前を付けてください。
4. インターフェイス（interface）を定義し、型エイリアスとの違いを確認してください。
5. typeof演算子を使った型ガードを実装してください。
6. instanceof演算子を使った型ガードを実装してください。
7. in演算子を使った型ガードを実装してください。
8. カスタム型ガード（is演算子）を使った関数を作成してください。
9. リテラル型（"success" | "error"）を使って、特定の値のみを受け付ける型を定義してください。
10. Enum（列挙型）を定義し、switch文で使用してください。
11. const enum を定義し、通常のenumとの違いを確認してください。
12. オプショナル型（T | undefined）とnullable型（T | null）を使い分けてください。
13. Non-null assertion operator（!）を使って、値がnullでないことを表明してください。
14. 型アサーション（as）を使って型を明示的にキャストしてください。
15. const assertion（as const）を使って、リテラル型を保持してください。
16. satisfies演算子を使って、型チェックしながら型推論を保持してください。
17. never型を使って、到達不可能なコードパスを表現してください。
18. unknown型を使って、型安全にany型を置き換えてください。
19. Discriminated Unions（判別可能なユニオン）を使って、型安全な状態管理を実装してください。
20. 型の互換性とstructural typing（構造的型付け）を確認するプログラムを書いてください。

### 10. クラスとOOP (20問)

1. 基本的なクラスを定義し、プロパティとメソッドを持たせてインスタンスを作成してください。
2. コンストラクタ（constructor）を持つクラスを定義し、プロパティを初期化してください。
3. アクセス修飾子（public、private、protected）を使ってカプセル化を実装してください。
4. getter と setter を使ってプロパティへのアクセスを制御してください。
5. readonly修飾子を使って、読み取り専用プロパティを定義してください。
6. staticプロパティとstaticメソッドを持つクラスを作成してください。
7. 継承（extends）を使って、基底クラスから派生クラスを作成してください。
8. super キーワードを使って、親クラスのコンストラクタとメソッドを呼び出してください。
9. メソッドのオーバーライドを行い、親クラスのメソッドを上書きしてください。
10. 抽象クラス（abstract class）を定義し、抽象メソッドを実装してください。
11. インターフェイスを実装する（implements）クラスを作成してください。
12. 複数のインターフェイスを実装するクラスを作成してください。
13. プロパティ初期化子（Property Initializers）を使ってプロパティを初期化してください。
14. パラメータプロパティ（constructor parameter properties）を使って簡潔にプロパティを定義してください。
15. クラス式（Class Expression）を使ってクラスを定義してください。
16. thisの型をクラス内で適切に扱うプログラムを書いてください。
17. クラスの型としてのインスタンス型とコンストラクタ型を理解し、使い分けてください。
18. インターフェイスでクラスの構造を定義し、そのインターフェイスを実装してください。
19. 抽象クラスとインターフェイスの使い分けを示すプログラムを書いてください。
20. メソッドチェーン（Method Chaining）をサポートするクラスを作成してください（thisを返す）。

### 11. タプルとEnum (10問)

1. 基本的なタプル型（[string, number]）を定義し、値を代入してください。
2. タプルの分割代入を使って、各要素を個別の変数に代入してください。
3. ラベル付きタプル（[name: string, age: number]）を定義し、可読性を向上させてください。
4. オプショナル要素を含むタプル（[string, number?]）を定義してください。
5. Rest要素を含むタプル（[string, ...number[]]）を定義してください。
6. readonlyタプルを定義し、要素の変更を防いでください。
7. タプルを関数の戻り値として使用し、複数の値を返してください。
8. 数値Enum（Numeric Enum）を定義し、値を確認してください。
9. 文字列Enum（String Enum）を定義し、型安全な文字列定数を作成してください。
10. Enumをkeyof typeofと組み合わせて、Enumのキーの型を取得してください。

### 12. ジェネリクス (20問)

1. ジェネリック関数を定義し、任意の型の配列を受け取って最初の要素を返してください。
2. 複数の型パラメータを持つジェネリック関数を作成してください。
3. ジェネリックインターフェイスを定義し、さまざまな型で使用してください。
4. ジェネリッククラスを定義し、型パラメータを持つプロパティとメソッドを実装してください。
5. 型パラメータに制約（extends）を加え、特定の型のみを受け付けるようにしてください。
6. 複数の制約を持つジェネリック型を定義してください（T extends A & B）。
7. デフォルト型パラメータを持つジェネリック型を定義してください。
8. ジェネリック型のエイリアスを作成してください。
9. keyof演算子とジェネリクスを組み合わせて、オブジェクトのキーを型安全に扱ってください。
10. ジェネリック型を使って、Stack（スタック）データ構造を実装してください。
11. ジェネリック型を使って、Queue（キュー）データ構造を実装してください。
12. ジェネリックなPromiseを返す非同期関数を作成してください。
13. ジェネリックなイベントエミッタークラスを実装してください。
14. typeof演算子とジェネリクスを組み合わせて使用してください。
15. ジェネリックなコンストラクタ型（new (...args: any[]) => T）を使用してください。
16. 条件付きジェネリクス（T extends U ? X : Y）の基本を理解して使用してください。
17. ジェネリック型パラメータをインターフェイスのメソッドでのみ使用してください。
18. ジェネリック型のバリアンス（共変性・反変性）を理解するプログラムを書いてください。
19. ジェネリック型を使って、型安全なビルダーパターンを実装してください。
20. 再帰的なジェネリック型を定義してください（例：連結リスト）。

### 13. ユーティリティ型 (15問)

1. Partial<T>を使って、すべてのプロパティをオプショナルにした型を作成してください。
2. Required<T>を使って、すべてのプロパティを必須にした型を作成してください。
3. Readonly<T>を使って、すべてのプロパティを読み取り専用にした型を作成してください。
4. Pick<T, K>を使って、特定のプロパティのみを選択した型を作成してください。
5. Omit<T, K>を使って、特定のプロパティを除外した型を作成してください。
6. Record<K, T>を使って、キーと値の型を指定したオブジェクト型を作成してください。
7. Exclude<T, U>を使って、ユニオン型から特定の型を除外してください。
8. Extract<T, U>を使って、ユニオン型から特定の型のみを抽出してください。
9. NonNullable<T>を使って、nullとundefinedを除外した型を作成してください。
10. ReturnType<T>を使って、関数の戻り値の型を取得してください。
11. Parameters<T>を使って、関数の引数の型をタプルとして取得してください。
12. ConstructorParameters<T>を使って、コンストラクタの引数の型を取得してください。
13. InstanceType<T>を使って、コンストラクタのインスタンス型を取得してください。
14. Awaited<T>を使って、Promiseがresolveする型を取得してください。
15. 複数のユーティリティ型を組み合わせて、複雑な型変換を行ってください。

### 14. 非同期処理 (15問)

1. Promise<T>型を明示的に指定した非同期関数を作成してください。
2. async/awaitを使って非同期処理を同期的に書いてください。
3. try-catchを使って非同期処理のエラーをハンドリングしてください。
4. Promise.all()を使って複数のPromiseを並列実行し、すべての結果を待ち受けてください。
5. Promise.race()を使って、最初に完了したPromiseの結果を取得してください。
6. Promise.allSettled()を使って、すべてのPromiseの結果（成功・失敗含む）を取得してください。
7. Promise.any()を使って、最初に成功したPromiseの結果を取得してください。
8. Promiseチェーン（then-catch-finally）を使って非同期処理を連鎖させてください。
9. async関数内でawaitを使わずにPromiseを返してください。
10. ジェネリック型を使った型安全な非同期関数を複数作成してください。
11. カスタムPromiseを作成し、resolve/rejectを手動で呼び出してください。
12. タイムアウト機能を持つPromiseラッパー関数を作成してください。
13. リトライ機能を持つ非同期関数を実装してください。
14. async/awaitとエラーハンドリングを組み合わせた堅牢な非同期処理を書いてください。
15. 非同期ジェネレーター（async function*）を使って、非同期イテレータを実装してください。

### 15. 高度な型システム (25問)

1. Conditional Types（条件型）の基本構文（T extends U ? X : Y）を使ってください。
2. Conditional Typesを使って、引数の型によって戻り値の型を変える関数を作成してください。
3. Mapped Types（マップ型）を使って、既存の型のすべてのプロパティを変換してください。
4. keyof演算子とMapped Typesを組み合わせて使用してください。
5. Template Literal Typesを使って、文字列リテラル型を組み合わせた新しい型を作成してください。
6. inferキーワードを使って、Conditional Types内で型を推論してください。
7. inferを使って、関数の戻り値の型や引数の型を抽出してください。
8. Discriminated Unions（判別可能なユニオン）を使って、型安全な状態管理を実装してください。
9. Index Signatures（[key: string]: T）を使って動的なプロパティを持つ型を定義してください。
10. Indexed Access Types（T[K]）を使って、型のプロパティの型にアクセスしてください。
11. 再帰的な型（Recursive Types）を定義してください（例：JSON型）。
12. 型レベルでの条件分岐を複数組み合わせた複雑な型を作成してください。
13. Mapped Typesでキー名を変更する型を作成してください（Template Literal Typesと組み合わせ）。
14. as句を使ったMapped Typesでキーを再マッピングしてください。
15. Variance（共変性・反変性・不変性）を理解し、適切な型設計を行ってください。
16. 型の分配法則（Distributive Conditional Types）を理解して使用してください。
17. ユーティリティ型（PartialやPickなど）を自分で実装してください。
18. 複雑なオブジェクトをDeep Partial（再帰的にPartial）にする型を作成してください。
19. 複雑なオブジェクトをDeep Readonly（再帰的にReadonly）にする型を作成してください。
20. Union型からIntersection型に変換する型を作成してください。
21. 関数のオーバーロードシグネチャを型レベルで表現してください。
22. 型レベルでの文字列操作（Uppercase、Lowercase、Capitalize、Uncapitalize）を使用してください。
23. Branded Types（ブランド型）を作成して、プリミティブ型を区別してください。
24. Opaque Types（不透明型）のパターンを実装してください。
25. 型パズルを解いてください（例：タプルの長さを数える型、配列を反転する型など）。

### 16. モジュールと型定義 (15問)

1. ES Modulesのimport/exportを使ってモジュールを作成してください。
2. 名前付きエクスポート（named export）とデフォルトエクスポート（default export）の違いを示してください。
3. import * as を使ってモジュール全体をインポートしてください。
4. 型のみのインポート（import type）を使用してください。
5. 型のみのエクスポート（export type）を使用してください。
6. namespaceを使ってコードを整理してください。
7. namespaceのネストと、ドット記法でのアクセスを実装してください。
8. 型定義ファイル（.d.ts）を作成し、JavaScriptライブラリに型を追加してください。
9. declare キーワードを使って、グローバル変数の型を宣言してください。
10. declare module を使って、外部モジュールの型を拡張してください。
11. Triple-Slash Directives（/// <reference path="..." />）を使用してください。
12. @types パッケージの仕組みを理解し、適切に使用してください。
13. モジュール解決戦略（moduleResolution）の違いを理解してください（Node vs Classic）。
14. paths と baseUrl を使ってモジュールのエイリアスを設定してください。
15. ambient declarations（環境宣言）を使って、外部ライブラリの型を定義してください。

### 17. デコレータ (10問)

1. experimentalDecoratorsを有効にして、クラスデコレータを作成してください。
2. メソッドデコレータを作成し、メソッドの実行をログ出力してください。
3. プロパティデコレータを作成し、プロパティのメタデータを追加してください。
4. パラメータデコレータを作成し、引数の情報を記録してください。
5. アクセサデコレータ（getter/setterデコレータ）を作成してください。
6. デコレータファクトリー（パラメータを受け取るデコレータ）を実装してください。
7. 複数のデコレータを1つのクラス/メソッドに適用し、実行順序を確認してください。
8. デコレータを使って、メソッドの実行時間を計測する機能を実装してください。
9. デコレータを使って、メソッドの引数と戻り値のバリデーションを実装してください。
10. reflect-metadataライブラリを使って、メタデータを保存・取得してください。

### 18. ファイル・データ処理 (15問)

1. Node.jsのfsモジュールを使って、テキストファイルを同期的に読み込んでください。
2. fs.promises を使って、ファイルを非同期的に読み込んでください。
3. ファイルに文字列を書き込むプログラムを書いてください（同期・非同期の両方）。
4. ファイルの存在確認を行い、存在する場合のみ読み込むプログラムを書いてください。
5. JSON.parse()とJSON.stringify()を型安全に使用してください（ジェネリクスを活用）。
6. JSONファイルを読み込み、型安全にパースしてください（zod等のバリデーションライブラリは使わず）。
7. 型定義とともにJSONデータをファイルに書き込んでください。
8. CSVデータを読み込み、オブジェクトの配列に変換してください。
9. オブジェクトの配列をCSV形式の文字列に変換してください。
10. ディレクトリ内のファイル一覧を取得し、型安全に処理してください。
11. Path モジュールを使って、パスの操作を型安全に行ってください。
12. ストリームを使って大きなファイルを効率的に処理してください。
13. ファイルの読み書きでエラーハンドリングを適切に実装してください。
14. バイナリファイルを読み込み、Bufferとして処理してください。
15. 複数のファイルを並列で読み込み、Promise.all()で結果を集約してください。

### 19. 正規表現とエラーハンドリング (10問)

1. RegExpオブジェクトを使って、文字列が有効なメールアドレスかどうかを判定してください。
2. String.match()を使って、文字列からすべての数字を抽出してください。
3. String.replace()と正規表現を使って、パターンに一致する部分を置換してください。
4. test()メソッドとexec()メソッドの違いを理解して使い分けてください。
5. グローバルフラグ（g）とマルチラインフラグ（m）を使った正規表現を作成してください。
6. カスタムエラークラスを作成し、Errorクラスを継承してください。
7. try-catch-finally を使って、エラーハンドリングとクリーンアップ処理を実装してください。
8. 型ガードを使って、エラーの型を判別して処理してください。
9. Result型（Success | Failure）を実装して、エラーを型安全に扱ってください。
10. async/awaitとtry-catchを組み合わせて、非同期処理のエラーハンドリングを実装してください。

### 20. TypeScriptベストプラクティス (15問)

1. 型推論を活用し、不要な型注釈を省いたコードを書いてください。
2. any型を使わずに、unknown型やジェネリクスで型安全性を保ってください。
3. strictモードを有効にし、すべてのstrictオプションの効果を確認してください。
4. noImplicitAny の重要性を理解し、適切に型を付けてください。
5. strictNullChecks を有効にして、null/undefinedを安全に扱ってください。
6. noUnusedLocals と noUnusedParameters を有効にして、未使用変数を検出してください。
7. 型アサーションの使いすぎを避け、型ガードや型推論を優先してください。
8. interfaceとtypeの使い分けを理解し、適切に使用してください。
9. enumの代わりにUnion Typesを使用する場面を判断してください。
10. const assertionとreadonly を使って、イミュータブルなデータ構造を作成してください。
11. ジェネリクスの型パラメータに適切な制約を付けてください。
12. コンパイラオプション（target、module、lib等）を理解して適切に設定してください。
13. コードの可読性を高めるために、型エイリアスに適切な名前を付けてください。
14. 循環参照を避け、モジュール設計を適切に行ってください。
15. パフォーマンスを考慮した型定義を行ってください（過度に複雑な型を避ける）。

### 21. 実践的な演習問題 (10問)

1. FizzBuzz問題：1から100までの数をプリントするプログラムを型安全に書いてください。3の倍数のときは"Fizz"と、5の倍数のときは"Buzz"とプリントしてください。3と5両方の倍数の場合には"FizzBuzz"とプリントしてください。
2. 型安全なToDoリストクラスを実装してください。タスクの追加、削除、完了マーク、フィルタリング機能を持たせてください。
3. ジェネリックなLinkedList（連結リスト）データ構造を実装してください。
4. 型安全なEventEmitter（イベントエミッター）を実装してください。イベント名と引数の型を厳密にチェックしてください。
5. 型安全なステートマシンを実装してください。状態遷移と各状態で許可されるアクションを型で表現してください。
6. ビルダーパターンを型安全に実装してください。メソッドチェーンで段階的にオブジェクトを構築できるようにしてください。
7. 素数判定プログラムを型安全に実装してください。引数と戻り値に適切な型を付けてください。
8. シーザー暗号を実装してください。暗号化と復号化の両方を型安全に実装してください。
9. 型安全なコマンドラインパーサーを実装してください。引数を解析してオブジェクトとして返してください。
10. 簡易的なテンプレートエンジンを実装してください。テンプレート文字列と変数のマッピングを型安全に行ってください。
