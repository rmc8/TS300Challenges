/**
 * Challenge 161: keyof制約
 *
 * keyof を使って、オブジェクトのキーに制約を付けてください。
 *
 * 学習ポイント:
 * - keyof 制約
 * - <K extends keyof T>
 * - 型安全なプロパティアクセス
 * - オブジェクトキーの型
 *
 * @example
 * challenge161();
 * // 出力: Taro
 */

export function challenge161(): void {
  // TODO: function getProperty<T, K extends keyof T>(obj: T, key: K): T[K] の形式で関数を宣言してください
  // TODO: obj[key] を返してください
  // TODO: オブジェクトとキーで関数を呼び出してconsole.log()で出力してください
}
