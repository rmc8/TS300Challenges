/**
 * Challenge 228: type-only import/export
 *
 * type-only import/export を使って、型のみをインポート/エクスポートしてください。
 *
 * 学習ポイント:
 * - import type 構文
 * - export type 構文
 * - 型のみのインポート
 * - ランタイムへの影響なし
 *
 * @example
 * challenge228();
 * // 出力: Type-only import
 */

export function challenge228(): void {
  // TODO: import type { TypeName } を使って型のみをインポートしてください
  // TODO: export type { TypeName } を使って型のみをエクスポートしてください
  // TODO: 結果をconsole.log()で出力してください
}
