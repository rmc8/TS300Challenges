/**
 * Challenge 159: Promise<T>
 *
 * Promise<T> を使って、非同期処理の型を定義してください。
 *
 * 学習ポイント:
 * - Promise<T>
 * - ジェネリックな非同期処理
 * - async/await との組み合わせ
 * - 型安全な非同期関数
 *
 * @example
 * challenge159();
 * // 出力: 42
 */

export function challenge159(): void {
  // TODO: function fetchData<T>(data: T): Promise<T> の形式で関数を宣言してください
  // TODO: Promise.resolve(data) を返してください
  // TODO: async/await で関数を呼び出してconsole.log()で出力してください
}
