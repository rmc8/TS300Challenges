# CLAUDE.md

このファイルは、このリポジトリで作業する際にClaude Code (claude.ai/code) へのガイダンスを提供します。

## プロジェクト概要

TS300Challengesは、難易度別に整理された300個のコーディングチャレンジを含むTypeScript学習リポジトリです。これはDart300ChallengesプロジェクトのTypeScript版であり、開発者がTypeScriptの構文、型、アルゴリズム、問題解決を実践的に学ぶために設計されています。

## コマンド

### 開発
- `npm install` - すべての依存関係をインストール
- `npm run build` - TypeScriptをJavaScriptにコンパイル（`dist/`に出力）
- `npm run lint` - ESLintでコード品質をチェック
- `npm run lint:fix` - ESLintの問題を自動修正
- `npm run format` - Prettierでコードをフォーマット
- `npm run format:check` - コードが正しくフォーマットされているかチェック

### テスト
- `npm test` - すべてのテストを実行
- `npm run test:watch` - ウォッチモードでテストを実行（ファイル変更時に自動再実行）
- `npm run test:coverage` - テストカバレッジレポートを生成
- `npm test -- <filename>` - 特定のテストファイルを実行（例: `npm test -- challenge001`）
- `npm test -- -t "<test name>"` - 名前を指定して特定のテストを実行

## アーキテクチャ

### ディレクトリ構造
```
src/
├── challenges/
│   ├── 001-050/    # 基礎レベルのチャレンジ
│   ├── 051-100/    # 初級レベルのチャレンジ
│   ├── 101-150/    # 中級レベルのチャレンジ
│   ├── 151-200/    # 上級レベルのチャレンジ
│   ├── 201-250/    # エキスパートレベルのチャレンジ
│   └── 251-300/    # マスターレベルのチャレンジ
└── utils/          # 共有ユーティリティ関数と型
```

### チャレンジファイルの命名規則
各チャレンジは以下のパターンに従います:
- `challengeXXX.ts` - チャレンジの実装
- `challengeXXX.test.ts` - 対応するテストファイル

チャレンジは3桁のゼロパディング番号（001-300）で番号付けされ、50個ずつのフォルダーにグループ化されています。

### チャレンジの構造
各チャレンジファイルは以下をエクスポートする必要があります:
1. 問題を解決する明確な関数またはクラス
2. 適切なTypeScript型注釈
3. 問題と解決アプローチを説明するJSDocコメント

各テストファイルには以下を含める必要があります:
1. エッジケースをカバーする複数のテストケース
2. 明確なテストの説明
3. シンプルな入力シナリオと複雑な入力シナリオの両方

## TypeScript設定

このプロジェクトは厳格なTypeScript設定を使用しています:
- `strict: true` - すべての厳格な型チェックオプションを有効化
- `noUnusedLocals: true` - 未使用のローカル変数でエラー
- `noUnusedParameters: true` - 未使用のパラメータでエラー
- `noImplicitReturns: true` - すべてのコードパスが値を返さない場合にエラー
- `noFallthroughCasesInSwitch: true` - switchのフォールスルーでエラー

新しいチャレンジを追加する際は、すべての型注釈を明示的にし、TypeScriptの型システムを最大限に活用してください（ジェネリクス、ユニオン型、型ガードなど）。

## テストの方針

- 各チャレンジには包括的なテストが必要
- テストは正確性と型安全性の両方を検証する必要がある
- TypeScriptの型をテストする際はJestの型アサーションユーティリティを使用
- エッジケースを含める: 空の入力、null/undefined、大きな数値、境界条件
- テストの説明は何を検証しているかを明確に説明する必要がある

## 新しいチャレンジの追加

1. 適切な難易度のフォルダーを決定（001-050は基礎レベル、など）
2. 解答を含む`challengeXXX.ts`を作成
3. 包括的なテストを含む`challengeXXX.test.ts`を作成
4. チャレンジに以下が含まれていることを確認:
   - コメントでの明確な問題の説明
   - 型安全な実装
   - エクスポートされた関数/クラス
   - 完全なテストカバレッジ
5. `npm test -- challengeXXX`を実行してテストが合格することを確認
6. `npm run lint`を実行してコード品質を確認
7. `npm run format`を実行して一貫したフォーマットを確保

## 参考ドキュメント

TypeScript公式ドキュメント（日本語）: https://www.typescriptlang.org/ja/docs/
