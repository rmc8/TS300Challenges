/**
 * Challenge 177: Exclude<T, U>
 *
 * Exclude<T, U> を使って、ユニオン型から特定の型を除外してください。
 *
 * 学習ポイント:
 * - Exclude<T, U>
 * - ユニオン型から型を除外
 * - 型の差分
 * - 条件型の応用
 *
 * @example
 * challenge177();
 * // 出力: b
 */

export function challenge177(): void {
  // TODO: type AllKeys = 'a' | 'b' | 'c' を定義してください
  // TODO: type ExcludedKeys = Exclude<AllKeys, 'a' | 'c'> を定義してください
  // TODO: ExcludedKeys型の変数を宣言してconsole.log()で出力してください
}
