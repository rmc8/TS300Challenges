/**
 * Challenge 099: 純粋関数
 *
 * 純粋関数（Pure Function）の例を作成してください。
 *
 * 学習ポイント:
 * - 純粋関数
 * - 副作用なし
 * - 参照透過性
 * - 関数型プログラミングの原則
 *
 * @example
 * challenge099();
 * // 出力: 10
 * // 出力: 10
 */

export function challenge099(): void {
  // TODO: 純粋関数を定義してください（同じ入力に対して常に同じ出力を返す）
  // TODO: 副作用を持たないことを確認してください
  // TODO: 同じ引数で複数回呼び出して結果をconsole.log()で出力してください
}
