/**
 * Challenge 181: Parameters<T>
 *
 * Parameters<T> を使って、関数のパラメータの型をタプルとして取得してください。
 *
 * 学習ポイント:
 * - Parameters<T>
 * - 関数パラメータの型抽出
 * - タプル型への変換
 * - 関数シグネチャの分解
 *
 * @example
 * challenge181();
 * // 出力: ['Taro', 25]
 */

export function challenge181(): void {
  // TODO: function greet(name: string, age: number): void { } を定義してください
  // TODO: type GreetParams = Parameters<typeof greet> を定義してください
  // TODO: GreetParams型の変数を宣言してconsole.log()で出力してください
}
