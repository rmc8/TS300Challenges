/**
 * Challenge 184: ThisParameterType<T>
 *
 * ThisParameterType<T> を使って、関数のthisパラメータの型を取得してください。
 *
 * 学習ポイント:
 * - ThisParameterType<T>
 * - thisパラメータの型抽出
 * - 明示的なthis型
 * - 関数のthisコンテキスト
 *
 * @example
 * challenge184();
 * // 出力: { value: number }
 */

export function challenge184(): void {
  // TODO: function getValue(this: { value: number }): number { return this.value; } を定義してください
  // TODO: type ThisType = ThisParameterType<typeof getValue> を定義してください
  // TODO: ThisType型のオブジェクトを作成してconsole.log()で出力してください
}
