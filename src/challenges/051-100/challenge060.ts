/**
 * Challenge 060: includes()とsome()
 *
 * includes()とsome()を使って配列が特定の条件を満たすかどうかを
 * 判定してください。
 *
 * 学習ポイント:
 * - includes()：特定の値が含まれるか
 * - some()：条件を満たす要素が1つでもあるか
 * - boolean値の返却
 *
 * @example
 * challenge060();
 * // 出力:
 * // includes: true
 * // some: true
 */

export function challenge060(): void {
  // TODO: number[]型の配列を宣言し、値を設定してください
  // TODO: includes()で特定の値が含まれるかチェックして出力してください
  // TODO: some()で条件（例: x > 10）を満たす要素があるかチェックして出力してください
}
