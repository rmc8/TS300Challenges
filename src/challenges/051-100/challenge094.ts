/**
 * Challenge 094: IIFE（即時実行関数式）
 *
 * IIFE（Immediately Invoked Function Expression）を使って、
 * 即座に実行される関数を作成してください。
 *
 * 学習ポイント:
 * - IIFE
 * - 即時実行関数式
 * - スコープの分離
 * - グローバル汚染の防止
 *
 * @example
 * challenge094();
 * // 出力: IIFE executed!
 */

export function challenge094(): void {
  // TODO: (() => { ... })() の形式でIIFEを作成してください
  // TODO: IIFE内でconsole.log()を呼び出してください
}
