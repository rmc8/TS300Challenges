/**
 * Challenge 058: 配列の最大値と最小値
 *
 * Math.max(...array)とMath.min(...array)を使って配列の最大値と
 * 最小値を求めてください。
 *
 * 学習ポイント:
 * - スプレッド演算子による配列の展開
 * - Math.max()とMath.min()
 * - 可変長引数の活用
 *
 * @example
 * challenge058();
 * // 出力:
 * // 最大値: 10
 * // 最小値: 1
 */

export function challenge058(): void {
  // TODO: number[]型の配列を宣言し、複数の値を設定してください
  // TODO: Math.max(...array)で最大値を求めて出力してください
  // TODO: Math.min(...array)で最小値を求めて出力してください
}
