/**
 * Challenge 100: 再帰関数
 *
 * 再帰関数を使って、階乗を計算してください。
 *
 * 学習ポイント:
 * - 再帰関数
 * - ベースケース
 * - 再帰呼び出し
 * - スタックオーバーフロー対策
 *
 * @example
 * challenge100();
 * // 出力: 120 (5の階乗)
 */

export function challenge100(): void {
  // TODO: function factorial(n: number): number の形式で再帰関数を宣言してください
  // TODO: n === 0 または n === 1 をベースケースとして定義してください
  // TODO: それ以外の場合は n * factorial(n - 1) を返してください
  // TODO: factorial(5) を呼び出して結果をconsole.log()で出力してください
}
