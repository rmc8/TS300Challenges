/**
 * Challenge 103: never型
 *
 * never型を使って、決して値を返さない関数を定義してください。
 *
 * 学習ポイント:
 * - never型
 * - 到達不可能なコード
 * - 例外をスローする関数
 * - 無限ループ
 *
 * @example
 * challenge103();
 * // エラーがスローされる
 */

export function challenge103(): void {
  // TODO: function throwError(message: string): never の形式で関数を宣言してください
  // TODO: 関数内で throw new Error(message) を実行してください
  // TODO: try-catch でエラーをキャッチしてconsole.log()で出力してください
}
