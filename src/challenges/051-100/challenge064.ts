/**
 * Challenge 064: every()で全要素チェック
 *
 * every()を使って配列のすべての要素が特定の条件を満たすかチェックしてください。
 *
 * 学習ポイント:
 * - every()メソッド
 * - 全要素が条件を満たすかの判定
 * - boolean値の返却
 * - 短絡評価
 *
 * @example
 * challenge064();
 * // 出力: true または false
 */

export function challenge064(): void {
  // TODO: number[]型の配列を宣言し、値を設定してください
  // TODO: every()を使って全要素が正の数かチェックしてください
  // TODO: array.every(x => x > 0) の形式を使用してください
  // TODO: 結果（boolean）をconsole.log()で出力してください
}
