/**
 * Challenge 156: ジェネリック制約（extends）
 *
 * extends を使って、ジェネリック型パラメータに制約を付けてください。
 *
 * 学習ポイント:
 * - ジェネリック制約
 * - <T extends Type>
 * - 制約による型の絞り込み
 * - インターフェースによる制約
 *
 * @example
 * challenge156();
 * // 出力: 5
 */

export function challenge156(): void {
  // TODO: interface Lengthwise を定義してください（length: number プロパティを持つ）
  // TODO: function getLength<T extends Lengthwise>(obj: T): number の形式で関数を宣言してください
  // TODO: obj.length を返してください
  // TODO: 文字列や配列で関数を呼び出してconsole.log()で出力してください
}
