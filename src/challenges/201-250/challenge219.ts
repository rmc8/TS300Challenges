/**
 * Challenge 219: async ジェネレータ
 *
 * async ジェネレータ関数を使って、非同期にデータを生成してください。
 *
 * 学習ポイント:
 * - async ジェネレータ
 * - async function*
 * - yield での非同期値生成
 * - AsyncGenerator<T>
 *
 * @example
 * challenge219();
 * // 出力: 0, 1, 2, 3, 4
 */

export function challenge219(): void {
  // TODO: async function* asyncRange(start: number, end: number): AsyncGenerator<number> を実装してください
  // TODO: yield で値を生成してください
  // TODO: for await...of でイテレートしてください
  // TODO: 各値をconsole.log()で出力してください
}
